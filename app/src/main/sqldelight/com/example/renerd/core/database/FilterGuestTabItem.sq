import kotlin.Boolean;

CREATE TABLE FilterGuestTabItem (
    label TEXT PRIMARY KEY,
    status INTEGER AS Boolean DEFAULT 0
);


-- Query para pegar todos os episódios
selectAllFilterGuestTabItem:
SELECT * FROM FilterGuestTabItem;

-- Query para inserir um novo episódio (ou atualizar se já existir)
insertFilterGuestTabItem:
INSERT OR REPLACE INTO FilterGuestTabItem (
    label,
    status
) VALUES (?, ?);